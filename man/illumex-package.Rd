% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/illumex-package.R
\docType{package}
\name{illumex-package}
\alias{illumex}
\alias{illumex-package}
\title{illumex: Describe, Clean, Plot and Profile Data Before a Model}
\description{
The exploratory half of the \code{illume} workflow, usable on its own: the
routine work before a model, each step one call with a consistent
interface. \code{illume} attaches this package, so with \code{illume} loaded
everything here is available as it always was.
}
\section{What is in it}{

\describe{
\item{Describing}{\code{\link[=ilm_describe_all]{ilm_describe_all()}} summarises every column of a data
frame at once, with a gaussian index in place of a normality test;
\code{\link[=ilm_counts]{ilm_counts()}}, \code{\link[=ilm_dupes]{ilm_dupes()}}, \code{\link[=ilm_copies]{ilm_copies()}} and \code{\link[=ilm_frame_issues]{ilm_frame_issues()}}
find what is wrong with a data frame before any model sees it.}
\item{Cleaning}{\code{\link[=ilm_wash_df]{ilm_wash_df()}}, \code{\link[=ilm_recode_errors]{ilm_recode_errors()}}, \code{\link[=ilm_translate]{ilm_translate()}}.}
\item{Uncertainty without a model}{\code{\link[=ilm_boot_ci]{ilm_boot_ci()}} and \code{\link[=ilm_boot_diff]{ilm_boot_diff()}}.}
\item{Unusual values}{\code{\link[=ilm_outliers]{ilm_outliers()}} for a value extreme in its own
column; \code{\link[=ilm_anomaly]{ilm_anomaly()}} and \code{\link[=ilm_plot_anomaly]{ilm_plot_anomaly()}} for a row implausible
as a combination.}
\item{Structure}{\code{\link[=ilm_profile]{ilm_profile()}} reduces, clusters and describes the
clusters in one call, for numeric and mixed columns alike;
\code{\link[=ilm_reduce]{ilm_reduce()}}, \code{\link[=ilm_cluster]{ilm_cluster()}} and \code{\link[=ilm_glrm]{ilm_glrm()}} are its parts.}
\item{Missing values}{\code{\link[=ilm_check_missing]{ilm_check_missing()}}, \code{\link[=ilm_describe_na]{ilm_describe_na()}} and
\code{\link[=ilm_plot_missing]{ilm_plot_missing()}} describe them. Imputing and pooling them is
\code{illume}'s: see \code{illume::ilm_impute()}.}
\item{Plots}{\code{\link[=ilm_plot]{ilm_plot()}} and the \verb{ilm_plot_*()} family, built on
\code{tinyplot} and named for what they show.}
\item{Example data}{\code{\link[=ilm_sim]{ilm_sim()}}, a grouped data set with a known
structure.}
}
}

\section{Two spellings}{

Every exported function whose name starts \code{ilm_} also answers to \code{iml_}, an
easy transposition to type: \code{iml_describe_all()} is \code{\link[=ilm_describe_all]{ilm_describe_all()}}
itself, not a wrapper, and opens the same help page.
}

\section{Lineage}{

This is a direct descendant of \code{elucidate}, by the same author, rebuilt on
\code{collapse} and \code{tinyplot}. The two share an interface rather than an
implementation.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/huttoncp/illumex}
  \item \url{https://huttoncp.github.io/illumex/}
  \item Report bugs at \url{https://github.com/huttoncp/illumex/issues}
}

}
\author{
\strong{Maintainer}: Craig Hutton \email{craig.hutton@gmail.com}

}
\keyword{internal}
