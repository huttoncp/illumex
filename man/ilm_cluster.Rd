% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_cluster.R, R/zzz-iml-aliases.R
\name{ilm_cluster}
\alias{ilm_cluster}
\alias{iml_cluster}
\title{Cluster observations, choosing the number of clusters}
\usage{
ilm_cluster(
  x,
  k = NULL,
  k_max = 10,
  method = c("kmeans", "hclust"),
  dist_method = "euclidean",
  hclust_method = "ward.D2",
  nstart = 25,
  B = 100,
  gap_method = "firstSEmax",
  small_cluster_frac = 0.05,
  ambiguous_threshold = 0.1,
  seed = NULL,
  progress = NULL
)
}
\arguments{
\item{x}{An \code{\link[=ilm_reduce]{ilm_reduce()}} result, or a numeric matrix or data frame of
coordinates with one row per observation.}

\item{k}{Number of clusters. Omit to choose it by the gap statistic.}

\item{k_max}{Largest \code{k} to consider in that search. Ignored when \code{k} is
given.}

\item{method}{\code{"kmeans"} or \code{"hclust"}.}

\item{dist_method}{Distance for \code{method = "hclust"}. Euclidean by default,
which is right here because the clustering runs on continuous
dimension-reduction coordinates rather than raw mixed-type columns.}

\item{hclust_method}{Linkage for \code{method = "hclust"}; \code{"ward.D2"} by
default.}

\item{nstart}{Random starts for k-means, guarding against a poor local
optimum.}

\item{B}{Bootstrap resamples, used both for the gap statistic and for the
stability score.}

\item{gap_method}{Rule for reading \code{k} off the gap curve; see
\code{\link[cluster:clusGap]{cluster::maxSE()}}.}

\item{small_cluster_frac}{Clusters holding less than this fraction of the
data are flagged.}

\item{ambiguous_threshold}{Silhouette width below which an observation is
flagged individually.}

\item{seed}{Random seed.}

\item{progress}{Show a progress bar. Defaults to \code{\link[=interactive]{interactive()}}, so a
bar appears when someone is watching and nothing is written in a
script or a knitted document. See \link{ilm_progress_arg}.}
}
\value{
An object of class \code{"ilm_cluster"}: \code{method}, \code{k}, \code{gap} (the
search, or \code{NULL} when \code{k} was given), \code{clusters} (one row per cluster,
with \code{size}, \code{pct}, \code{jaccard}, \code{stability} labelled \code{"stable"} above 0.75,
\code{"moderate"} from 0.5 to 0.75 and \code{"unstable"} below, following Hennig,
plus \code{mean_silhouette} and \code{anomalous}), \code{ind_cluster} (one row per
observation, with \code{silhouette}, \code{is_small_cluster}, \code{is_ambiguous} and
\code{is_anomalous}), and \code{coords}.
}
\description{
Groups the rows of an \code{\link[=ilm_reduce]{ilm_reduce()}} result, or any numeric coordinates, by
k-means or hierarchical clustering. When \code{k} is not given it is searched for
over \verb{1..k_max} by the gap statistic. Every cluster gets a stability score
from bootstrap resampling, every observation gets a silhouette width, and
clusters holding less than \code{small_cluster_frac} of the data are flagged.
}
\section{Two different things called anomalous}{


A \strong{small cluster} is a property of the cluster: a handful of rows that sit
apart from everything else. It may be a real minority pattern or it may be a
data problem, and either way it is worth looking at.

An \strong{ambiguous observation} is a property of the row, taken from its
silhouette width -- its average distance to its own cluster against its
average distance to the nearest other one. Near 1 means clearly placed, near
0 means sitting on a boundary, negative means it is closer to another cluster
than to its own. This is independent of cluster size: a point can be
ambiguous inside a large and otherwise stable cluster, which a size-based
flag alone would never show. Kaufman and Rousseeuw treat widths below 0.25 as
no substantial structure; the default threshold here is stricter at 0.1.
}

\examples{
cl <- ilm_cluster(ilm_reduce(mtcars), k_max = 5, B = 25, seed = 1)
cl
}
\references{
Hennig, C. (2007). Cluster-wise assessment of cluster stability.
Computational Statistics and Data Analysis 52(1).

Tibshirani, R., Walther, G. and Hastie, T. (2001). Estimating the number of
clusters in a data set via the gap statistic. JRSS B 63(2).
}
\seealso{
\code{\link[=ilm_reduce]{ilm_reduce()}}, \code{\link[=ilm_profile]{ilm_profile()}}, \code{\link[=ilm_plot_cluster]{ilm_plot_cluster()}}.
}
