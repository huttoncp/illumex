% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_anomalous.R, R/zzz-iml-aliases.R
\name{ilm_anomalous}
\alias{ilm_anomalous}
\alias{iml_anomalous}
\title{The rows an anomaly scan flagged}
\usage{
ilm_anomalous(x, data = NULL, flagged = TRUE, score = TRUE)
}
\arguments{
\item{x}{An \code{\link[=ilm_anomaly]{ilm_anomaly()}} result.}

\item{data}{The data frame that was scanned. Only needed if the result did
not keep it -- see \code{keep_data} in \code{\link[=ilm_anomaly]{ilm_anomaly()}}.}

\item{flagged}{Return only the flagged rows (the default), or all of them
with the score attached.}

\item{score}{Add the \code{score}, \code{p_adj} and \code{driver} columns.}
}
\value{
A data frame of the flagged rows, in their original order, with
\code{.row} giving their position in the input.
}
\description{
Pulls the flagged rows out of an \code{\link[=ilm_anomaly]{ilm_anomaly()}} result, as a data frame,
so they can be explored with anything that takes one.
}
\details{
The point of a scan is rarely the score. It is whether the odd rows are
alike: one mistake made repeatedly, a subpopulation the model does not
cover, or several unrelated things. That is a clustering or a description
question, and this is what hands the rows over.
}
\examples{
\donttest{
a <- ilm_anomaly(mtcars)
odd <- ilm_anomalous(a)
ilm_describe_all(odd)
}
}
\seealso{
\code{\link[=ilm_anomaly]{ilm_anomaly()}}, \code{\link[=ilm_profile]{ilm_profile()}}, \code{\link[=ilm_cluster]{ilm_cluster()}},
\code{\link[=ilm_describe_all]{ilm_describe_all()}}.
}
