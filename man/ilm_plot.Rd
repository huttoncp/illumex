% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot.R, R/zzz-iml-aliases.R
\name{ilm_plot}
\alias{ilm_plot}
\alias{iml_plot}
\title{Adaptive plot of one or two variables}
\usage{
ilm_plot(
  data,
  x,
  y = NULL,
  by = NULL,
  geom = "auto",
  colour = NULL,
  color = NULL,
  fill = NULL,
  alpha = NULL,
  size = NULL,
  palette = NULL,
  theme = NULL,
  n_max = 5000L,
  max_levels = 20L,
  verdict = TRUE,
  main = NULL,
  ...,
  pch = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Name of the variable on the horizontal axis.}

\item{y}{Optional second variable; required by some geoms.}

\item{by}{Optional grouping variable, drawn as colour with a legend.}

\item{geom}{\code{"auto"} or one of the geoms in the table above.}

\item{colour, color}{Colour of lines, points and borders. Synonyms; give one.}

\item{fill}{Fill colour for bars, boxes, violins and densities.}

\item{alpha}{Opacity between 0 (transparent) and 1 (opaque).}

\item{size}{Point size for \code{"point"}, line or border width elsewhere.}

\item{palette}{Colours for groups: a vector of colour names, or the name of
a palette such as \code{"Dark 2"}.}

\item{theme}{A tinyplot theme name, such as \code{"clean"}. With tinyplot 0.7.0
or later the available names are listed by \code{tinyplot::tinytheme_list()}
and an unknown one is reported here; on earlier versions tinyplot reports
it instead.}

\item{n_max}{Above this many points a scatter becomes a binned density.}

\item{max_levels}{Categorical levels beyond this are pooled into \code{(other)}.}

\item{verdict}{Annotate the plot with the diagnostic verdict.}

\item{main}{Plot title.}

\item{...}{Passed to \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}}.}

\item{pch}{Plotting character. Takes a NAME as well as a number:
\code{"filled circle"} is 16, and every code from 0 to 25 has one. Case,
spaces, underscores and hyphens are ignored. A single character is
drawn literally, so \code{pch = "x"} is still the letter x.}
}
\value{
Invisibly, a list with the geom used, the reason, and the note shown.
}
\description{
Chooses a plot appropriate to the classes of what you give it, annotates it
with the same diagnostic verdict \code{\link[=ilm_describe]{ilm_describe()}} reports, and switches to a
binned density when there are too many points to show individually.
}
\section{Which arguments each geom needs}{

\code{geom = "auto"} picks from the data. When you name a geom instead, some
require both \code{x} and \code{y} and some use \code{x} alone. \code{\link[=ilm_geom_spec]{ilm_geom_spec()}} returns
the full table, and it is the same table the argument checks read, so it
cannot drift from the behaviour:

\tabular{llll}{
\strong{geom} \tab \strong{x} \tab \strong{y} \tab \strong{\code{size} controls} \cr
histogram \tab numeric \tab not used \tab border width \cr
density \tab numeric \tab not used \tab line width \cr
bar \tab categorical or logical \tab not used \tab border width \cr
box \tab categorical or numeric \tab required \tab line width \cr
violin \tab categorical or numeric \tab required \tab line width \cr
point \tab numeric \tab required numeric \tab point size \cr
bin2d \tab numeric \tab required numeric \tab not used \cr
line \tab time or numeric \tab required \tab line width \cr
spine \tab categorical \tab required categorical \tab border width
}
}

\examples{
d <- ilm_sim()
ilm_plot(d, "score")
ilm_plot(d, "grp", "score", geom = "violin", fill = "lightblue")
ilm_plot(d, "score", "income", by = "grp", alpha = 0.6)
}
\seealso{
\code{\link[=ilm_geom_spec]{ilm_geom_spec()}}, \code{\link[=ilm_pick_geom]{ilm_pick_geom()}}, \code{\link[=ilm_plot_all]{ilm_plot_all()}},
\code{illume::ilm_plot_model()}.
}
