% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_boot.R, R/zzz-iml-aliases.R
\name{ilm_boot_diff}
\alias{ilm_boot_diff}
\alias{ilm_boot_diff.formula}
\alias{ilm_boot_diff.data.frame}
\alias{iml_boot_diff}
\title{Bootstrap intervals for differences between groups}
\usage{
ilm_boot_diff(x, ...)

\method{ilm_boot_diff}{formula}(x, data = NULL, ...)

\method{ilm_boot_diff}{data.frame}(
  x,
  y = NULL,
  group = NULL,
  stat = "mean",
  R = 2000L,
  conf = 0.95,
  ci_type = "percentile",
  adjust = "max_t",
  ref = NULL,
  seed = NULL,
  progress = NULL,
  ...
)
}
\arguments{
\item{x}{A data frame, or a formula \code{y ~ g} with \code{data} supplied.}

\item{...}{Passed between methods.}

\item{data}{The data frame, when \code{x} is a formula.}

\item{y}{Name of the numeric column. Ignored when \code{x} is a formula.}

\item{group}{Name of the grouping column, or several to cross with
\code{\link[=interaction]{interaction()}}. Ignored when \code{x} is a formula.}

\item{stat}{\code{"mean"}, \code{"median"}, \code{"sd"}, \code{"var"}, or a function taking a
numeric vector.}

\item{R}{Bootstrap replicates.}

\item{conf}{Confidence level. Under \code{"max_t"} and \code{"bonferroni"} this is the
level of the whole family of comparisons, not of one interval.}

\item{ci_type}{\code{"percentile"}, \code{"bca"}, \code{"normal"} or \code{"basic"}. Applies to
\code{"bonferroni"} and \code{"none"}; \code{"max_t"} has its own construction.}

\item{adjust}{\code{"max_t"}, \code{"bonferroni"} or \code{"none"}; see Details.}

\item{ref}{Optional level to compare every other level against, giving
\code{J - 1} comparisons instead of \code{J * (J - 1) / 2}.}

\item{seed}{Random seed.}

\item{progress}{Show a progress bar. Defaults to \code{\link[=interactive]{interactive()}}, so a
bar appears when someone is watching and nothing is written in a
script or a knitted document. See \link{ilm_progress_arg}.}
}
\value{
One row per comparison, with \code{from}, \code{to}, \code{observed}, \code{lower},
\code{upper}, \code{p_value}, \code{p_adj}, \code{excludes_zero} and the settings used.
Differences are \code{to} minus \code{from}.
}
\description{
Reports each difference with its own interval, rather than a set of intervals
to compare by eye: judging a difference by whether separate intervals overlap
is conservative and lossy.
}
\details{
With more than two groups every pair is compared, and the intervals are
\strong{simultaneous by default}. Ten comparisons each at a nominal 95\% do not
jointly cover at 95\%, and reporting them as though they did is the usual way
a pairwise table misleads. \code{adjust} chooses how that is handled:
\itemize{
\item \code{"max_t"} (default) resamples all groups together, standardises each
comparison by its own bootstrap standard error, and takes the \code{conf}
quantile of the largest absolute standardised value across comparisons as
one critical value for all of them. This is the single-step studentized
maximum. It assumes neither normality nor a common variance, which is what
separates it from Tukey's range test. Its intervals are symmetric about the
observed difference, so \code{ci_type} does not apply to them.
\item \code{"bonferroni"} builds each interval at level \code{1 - (1 - conf) / m} in the
requested \code{ci_type}. More conservative than \code{"max_t"}, but it keeps the
shape of a percentile or BCa interval, which matters on skewed data.
\item \code{"none"} builds each interval at \code{conf}. Correct for one comparison,
optimistic for several; use it when the comparisons were chosen in advance.
}

With a single comparison there is nothing to adjust, and the \code{adjust} column
of the result reads \code{"none"} whatever was asked for.

\code{p_value} and \code{p_adj} are built the same way the interval is, so the two
cannot contradict each other: under \code{"max_t"} both come off the studentized
maximum, and otherwise both come off the bootstrap distribution directly.
They are read off the same \code{R} replicates from opposite directions -- a
quantile and a tail proportion -- so a comparison sitting within one
replicate of the critical value can still land either side. Raise \code{R} if a
result is that close to the line. With \code{ci_type} of \code{"basic"}, \code{"normal"} or
\code{"bca"} the correspondence is close rather than exact, since those reshape
the same replicates while the p-value does not.
}
\section{What this is calibrated for}{


Against \code{stats::TukeyHSD()} on normal, equal-variance, balanced data -- the
case Tukey is exactly right for -- the endpoints agree to 0.006 and the
adjusted p-values to 0.011. Family-wise error over six comparisons at group
sizes of 45 to 80 came to 0.068 on normal data with a common variance and
0.072 with variances differing fourfold, against a nominal 0.05.

Heavy skew at small n is the case to know about. On lognormal data with a
spread parameter up to 1.2 and those same group sizes, a SINGLE unadjusted
comparison already erred 0.090 of the time, and no \code{ci_type} moved it
(percentile 0.090, BCa 0.089, basic 0.093, normal 0.084). That is the
bootstrapped mean of a heavily skewed small sample, not the multiplicity
adjustment, which is still doing its work: those six comparisons reject
0.302 of the time unadjusted against 0.126 under \code{"max_t"}. It converges --
at group sizes of 300 to 500 the same design gives 0.049 per comparison and
0.062 family-wise.

\code{stat = "median"} is the remedy when the data look like that. On the same
lognormal design, under a null placing the medians rather than the means
together, it gave 0.043 per comparison and 0.050 family-wise.
}

\examples{
d <- ilm_sim()
## all pairs, simultaneous by default
ilm_boot_diff(d, "score", "grp", R = 300, seed = 1)
## the same thing through a formula
ilm_boot_diff(score ~ grp, data = d, R = 300, seed = 1)
## every level against one reference, on medians
ilm_boot_diff(score ~ grp, data = d, stat = "median", ref = "alpha",
              R = 300, seed = 1)
}
\references{
Efron, B. and Tibshirani, R. J. (1993). An Introduction to the Bootstrap.
Chapman and Hall.

Westfall, P. H. and Young, S. S. (1993). Resampling-Based Multiple Testing.
Wiley.
}
\seealso{
\code{\link[=ilm_boot_ci]{ilm_boot_ci()}} for a single group.
}
