% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_describe.R, R/zzz-iml-aliases.R
\name{ilm_describe}
\alias{ilm_describe}
\alias{iml_describe}
\title{Class-aware description of one variable}
\usage{
ilm_describe(
  data,
  y = NULL,
  by = NULL,
  digits = 3,
  gauss = c("index", "ks_d", "both", "none"),
  probs = c(0, 0.5, 1),
  skew = FALSE,
  kurt = FALSE,
  dispersion = TRUE,
  rare_n = 5L,
  cap = 0.12,
  min_n = 20L
)
}
\arguments{
\item{data}{A data frame, or a vector when \code{y} is \code{NULL}.}

\item{y}{Name of the column to summarise, or several names. With none,
\code{data} is taken as the thing to describe: a bare vector, or a data frame
whose columns are all of one kind. For a frame of mixed kinds use
\code{\link[=ilm_describe_all]{ilm_describe_all()}}, which returns one table per kind.}

\item{by}{Optional character vector of grouping columns.}

\item{digits}{Rounding for numeric columns, quantiles included.}

\item{gauss}{One of \code{"index"} (the 0-1 agreement index), \code{"ks_d"} (the raw
Kolmogorov distance behind it), \code{"both"}, or \code{"none"}.}

\item{probs}{Quantiles to report, as proportions. \code{c(0, 0.5, 1)} gives
\code{p0}, \code{p50} and \code{p100}; any vector works and the column names follow it.}

\item{skew, kurt}{Add moment-based skewness and excess kurtosis.}

\item{dispersion}{Add the variance-to-mean ratio for non-negative integer
variables. Meaningful only if you intend to model the variable as a count.}

\item{rare_n}{Levels with fewer observations than this count as rare.}

\item{cap, min_n}{Control the \code{gauss} index; see \code{\link[=ilm_gauss_check]{ilm_gauss_check()}}.}
}
\value{
A one-row data frame, or one row per group when \code{by} is used.
}
\description{
Summarises a vector, or one column of a data frame, with statistics chosen
for its class. Numeric variables also get a gaussian agreement index and,
where that is low, a plain-language reason.
}
\details{
The default output is deliberately narrow. Quartiles, skewness and kurtosis
are available on request, but \code{gauss} and \code{gauss_note} already say whether
they are worth asking for.
}
\examples{
d <- ilm_sim()
ilm_describe(d, "score")
ilm_describe(d, "income", probs = c(0.1, 0.5, 0.9))
ilm_describe(d, "score", by = "grp")
}
\seealso{
\code{\link[=ilm_describe_all]{ilm_describe_all()}} for every column, \code{\link[=ilm_gauss_check]{ilm_gauss_check()}} for the
index, \code{\link[=ilm_describe_na]{ilm_describe_na()}} for missingness.
}
