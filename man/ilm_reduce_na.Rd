% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_reduce.R, R/zzz-iml-aliases.R
\name{ilm_reduce_na}
\alias{ilm_reduce_na}
\alias{iml_reduce_na}
\title{Reduce a data frame's missingness pattern to a few dimensions}
\usage{
ilm_reduce_na(data, cols = NULL, ndim = 5)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Columns to use. A character vector of names, a
regular expression, a predicate function such as \code{is.numeric}, or
\code{NULL} for all of them -- see \link{ilm_selection}.}

\item{ndim}{Number of dimensions to keep.}
}
\value{
An object of class \code{"ilm_reduce_na"}, which is also an
\code{"ilm_reduce"} -- see \code{\link[=ilm_reduce]{ilm_reduce()}} for the shared structure. In
\code{var_contrib}, \code{sqload} means how strongly a column's \emph{missingness}
relates to a dimension, not its values.
}
\description{
The missingness counterpart to \code{\link[=ilm_reduce]{ilm_reduce()}}. Builds a present/missing
marker for every column, drops any whose missingness never varies, and
reduces the markers -- which, being two-level categorical variables, always
takes the MCA route. The dimensions that come back describe which columns
tend to go missing \emph{together}, which is what separates a block of variables
lost to one skipped section from values that went missing independently.
}
\examples{
r <- ilm_reduce_na(airquality)
r
}
\seealso{
\code{\link[=ilm_profile_na]{ilm_profile_na()}} for the whole pipeline,
\code{\link[=ilm_check_missing]{ilm_check_missing()}} for whether any of it matters to your model.
}
