% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_describe.R, R/zzz-iml-aliases.R
\name{ilm_describe_all}
\alias{ilm_describe_all}
\alias{iml_describe_all}
\title{Describe every column of a data frame}
\usage{
ilm_describe_all(
  data,
  by = NULL,
  digits = 3,
  gauss = c("index", "ks_d", "both", "none"),
  probs = c(0, 0.5, 1),
  skew = FALSE,
  kurt = FALSE,
  dispersion = TRUE,
  class = "all",
  rare_n = 5L,
  cap = 0.12,
  min_n = 20L
)
}
\arguments{
\item{data}{A data frame, or a vector when \code{y} is \code{NULL}.}

\item{by}{Optional character vector of grouping columns.}

\item{digits}{Rounding for numeric columns, quantiles included.}

\item{gauss}{One of \code{"index"} (the 0-1 agreement index), \code{"ks_d"} (the raw
Kolmogorov distance behind it), \code{"both"}, or \code{"none"}.}

\item{probs}{Quantiles to report, as proportions. \code{c(0, 0.5, 1)} gives
\code{p0}, \code{p50} and \code{p100}; any vector works and the column names follow it.}

\item{skew, kurt}{Add moment-based skewness and excess kurtosis.}

\item{dispersion}{Add the variance-to-mean ratio for non-negative integer
variables. Meaningful only if you intend to model the variable as a count.}

\item{class}{\code{"all"}, or one or more of \code{"numeric"}, \code{"categorical"},
\code{"logical"}, \code{"time"}.}

\item{rare_n}{Levels with fewer observations than this count as rare.}

\item{cap, min_n}{Control the \code{gauss} index; see \code{\link[=ilm_gauss_check]{ilm_gauss_check()}}.}
}
\value{
A named list of data frames, one per class present, plus \code{constant}
when any column has a single value; a single data frame if only one
section results.
}
\description{
Applies \code{\link[=ilm_describe]{ilm_describe()}} to each column and returns one table per class, so
columns with different summaries are not forced into a common shape.
}
\details{
Constant columns are split into their own \code{constant} section rather than
padding every other table with rows of \code{NA}: a variable with one value tells
you nothing a statistic can show, and it will break a model matrix.
}
\examples{
d <- ilm_sim()
ilm_describe_all(d)
ilm_describe_all(d, class = "numeric", skew = TRUE)
}
\seealso{
\code{\link[=ilm_frame_issues]{ilm_frame_issues()}} for problems belonging to pairs of columns.
}
