% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_explore.R, R/zzz-iml-aliases.R
\name{ilm_translate}
\alias{ilm_translate}
\alias{iml_translate}
\title{Recode a variable against a dictionary held as two vectors}
\usage{
ilm_translate(y, old, new, default = NA)
}
\arguments{
\item{y}{A vector to recode.}

\item{old}{Values of the old coding scheme.}

\item{new}{Replacements, the same length as \code{old}.}

\item{default}{Value for entries not found in \code{old}. \code{NA} by default.}
}
\value{
A vector of the same length as \code{y}.
}
\description{
The shape a lookup table takes when it comes out of a spreadsheet: one
vector of old codes, one of new, matched by position.
}
\details{
Values with no entry in \code{old} become \code{NA} by default, which is deliberate: a
silent pass-through would hide codes the dictionary does not cover. Use
\code{default} to change that.
}
\examples{
ilm_translate(c(1, 2, 3, 99), old = 1:3, new = c("low", "mid", "high"))
ilm_translate(c(1, 2, 99), old = 1:2, new = c(10, 20), default = -1)
}
\seealso{
\code{\link[=match]{match()}}, \code{\link[=ilm_recode_errors]{ilm_recode_errors()}}.
}
