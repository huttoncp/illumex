% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_profile.R, R/zzz-iml-aliases.R
\name{ilm_profile_na}
\alias{ilm_profile_na}
\alias{iml_profile_na}
\title{Profile which values are missing, and for whom}
\usage{
ilm_profile_na(
  data,
  cols = NULL,
  ndim = 5,
  ...,
  vtest_threshold = 1.96,
  top_n_vars = 2
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Columns to use. A character vector of names, a
regular expression, a predicate function such as \code{is.numeric}, or
\code{NULL} for all of them -- see \link{ilm_selection}.}

\item{ndim}{Number of dimensions to keep.}

\item{...}{Passed to \code{\link[=ilm_cluster_na]{ilm_cluster_na()}}.}

\item{vtest_threshold}{Smallest \verb{|vtest|} for a dimension to count.}

\item{top_n_vars}{How many top-loading indicators to name per dimension.}
}
\value{
An object of class \code{"ilm_profile_na"}, which is also an
\code{"ilm_profile"}.
}
\description{
The missingness counterpart to \code{\link[=ilm_profile]{ilm_profile()}}: runs \code{\link[=ilm_reduce_na]{ilm_reduce_na()}} then
\code{\link[=ilm_cluster_na]{ilm_cluster_na()}}, and describes each cluster of rows by which columns'
\emph{missingness} sets it apart -- "cluster 2 is characterised by dim 1 (missing:
income, missing: age)" rather than by those columns' values.
}
\details{
This is how a structured gap shows itself: a block of variables that go
missing together points at a shared cause, such as a section of a form
everyone in one group skipped, which is a different problem from values
going missing one at a time. \code{\link[=ilm_check_missing]{ilm_check_missing()}} then says whether any of
it threatens the model you intend to fit.
}
\examples{
p <- ilm_profile_na(airquality, k_max = 4, B = 25, seed = 1)
cat(p$summary, sep = "\n")
}
\seealso{
\code{\link[=ilm_check_missing]{ilm_check_missing()}}, \code{illume::ilm_impute()}, \code{\link[=ilm_profile]{ilm_profile()}}.
}
