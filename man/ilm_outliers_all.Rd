% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_outliers.R, R/zzz-iml-aliases.R
\name{ilm_outliers_all}
\alias{ilm_outliers_all}
\alias{iml_outliers_all}
\title{Flag unusual values across a data frame}
\usage{
ilm_outliers_all(
  data,
  by = NULL,
  cols = NULL,
  method = c("iqr", "mad", "zscore"),
  threshold = NULL,
  flagged_only = TRUE,
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{by}{Grouping column(s), as a character vector. Reference statistics,
and so the flags, are computed separately within each group.}

\item{cols}{Columns to use. A character vector of names, a
regular expression, a predicate function such as \code{is.numeric}, or
\code{NULL} for all of them -- see \link{ilm_selection}.}

\item{method}{\code{"iqr"}, \code{"mad"} or \code{"zscore"}.}

\item{threshold}{Flagging threshold. Defaults per rule: 1.5 for \code{"iqr"}
(the boxplot convention), 3.5 for \code{"mad"} (Iglewicz and Hoaglin), 3 for
\code{"zscore"}.}

\item{flagged_only}{Return only the flagged values. \code{FALSE} returns every
value's score.}

\item{na.rm}{Compute the reference statistics with missing values removed.
The result always has one row per element of \code{y}, with \code{NA} for \code{score}
and \code{is_outlier} wherever \code{y} is \code{NA}.}
}
\value{
A data frame with the \code{by} columns, \code{row_id} (the row's position in
\code{data}), \code{variable}, \code{value}, \code{score} and \code{is_outlier}.
}
\description{
Runs \code{\link[=ilm_outliers]{ilm_outliers()}} over every numeric column, optionally within groups,
and returns one row per flagged value with the row it came from.
}
\details{
Grouping matters more than it looks. A value can be perfectly ordinary for
its own group and extreme against the pooled distribution, so flagging
without \code{by} on data that has groups mostly rediscovers the groups.
}
\examples{
head(ilm_outliers_all(mtcars))
head(ilm_outliers_all(mtcars, by = "cyl", method = "mad"))
}
\seealso{
\code{\link[=ilm_outliers]{ilm_outliers()}}, \code{\link[=ilm_plot_box]{ilm_plot_box()}} to see them.
}
