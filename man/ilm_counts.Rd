% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_explore.R, R/zzz-iml-aliases.R
\name{ilm_counts}
\alias{ilm_counts}
\alias{iml_counts}
\title{Frequency counts of a vector's unique values}
\usage{
ilm_counts(y, n = "all", order = c("d", "a", "i"), na.rm = TRUE)
}
\arguments{
\item{y}{A vector.}

\item{n}{Number of rows to return, or \code{"all"}.}

\item{order}{\code{"d"} by count descending, \code{"a"} by count ascending, or \code{"i"}
by the value itself (factor level order, not label order).}

\item{na.rm}{Drop missing values before counting.}
}
\value{
A data frame with \code{value} and \code{n}.
}
\description{
Frequency counts of a vector's unique values
}
\examples{
d <- ilm_sim()
ilm_counts(d$grp)
ilm_counts(d$site, n = 3)
}
\seealso{
\code{\link[=ilm_counts_tb]{ilm_counts_tb()}} for both ends at once, \code{\link[=ilm_counts_all]{ilm_counts_all()}} for a
whole data frame.
}
