% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot_eda.R, R/zzz-iml-aliases.R
\name{ilm_plot_stat_error}
\alias{ilm_plot_stat_error}
\alias{iml_plot_stat_error}
\title{Group means or medians with an error bar}
\usage{
ilm_plot_stat_error(
  data,
  y,
  x,
  by = NULL,
  stat = c("mean", "median"),
  ...,
  pch = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{Name of the numeric column to summarise.}

\item{x}{Name of the column defining the groups.}

\item{by}{Optional secondary grouping column.}

\item{stat}{\code{"mean"} with its standard error, or \code{"median"} with the
quartiles.}

\item{...}{Passed to \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}}.}

\item{pch}{Plotting character. Takes a NAME as well as a number:
\code{"filled circle"} is 16, and every code from 0 to 25 has one. Case,
spaces, underscores and hyphens are ignored. A single character is
drawn literally, so \code{pch = "x"} is still the letter x.}
}
\value{
\code{NULL}, invisibly.
}
\description{
A summary per group with a measure of spread around it.
}
\details{
The two \code{stat} options show different things and are not interchangeable.
\code{"mean"} draws the standard error, which is about where the \emph{mean} is;
\code{"median"} draws the quartiles, which is about where the \emph{data} are. The
first shrinks as the sample grows and the second does not.

Overlapping error bars are a poor test of a difference -- they are
conservative and lossy. \code{\link[=ilm_boot_diff]{ilm_boot_diff()}} gives the difference itself with
its own interval.
}
\examples{
ilm_plot_stat_error(mtcars, "mpg", "cyl")
}
\seealso{
\code{\link[=ilm_boot_diff]{ilm_boot_diff()}} for the comparison this plot invites.
}
