% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_explore.R, R/zzz-iml-aliases.R
\name{ilm_counts_all}
\alias{ilm_counts_all}
\alias{iml_counts_all}
\title{Frequency counts for every column}
\usage{
ilm_counts_all(
  data,
  by = NULL,
  n = "all",
  order = c("d", "a", "i"),
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{by}{Optional grouping columns.}

\item{n}{Number of rows to return, or \code{"all"}.}

\item{order}{\code{"d"} by count descending, \code{"a"} by count ascending, or \code{"i"}
by the value itself (factor level order, not label order).}

\item{na.rm}{Drop missing values before counting.}
}
\value{
A data frame with \code{variable}, \code{value} and \code{n}.
}
\description{
Values are stacked across columns of different classes, so they are rendered
as character; the counts stay integer.
}
\examples{
ilm_counts_all(ilm_sim()[, c("grp", "site")], n = 2)
}
