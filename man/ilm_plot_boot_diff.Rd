% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot_multi.R, R/zzz-iml-aliases.R
\name{ilm_plot_boot_diff}
\alias{ilm_plot_boot_diff}
\alias{iml_plot_boot_diff}
\title{The bootstrap distribution behind a group difference}
\usage{
ilm_plot_boot_diff(
  x,
  row = 1L,
  type = c("density", "histogram"),
  ref_line = 0,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=ilm_boot_diff]{ilm_boot_diff()}} result.}

\item{row}{Which comparison to draw, when the result has several. The
default draws the first, and having more than one is normal now that
\code{\link[=ilm_boot_diff]{ilm_boot_diff()}} compares every pair.}

\item{type}{\code{"density"} or \code{"histogram"}.}

\item{ref_line}{Where to draw the reference line.}

\item{...}{Passed to \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Draws the replicate differences from one row of an \code{\link[=ilm_boot_diff]{ilm_boot_diff()}} result,
with zero and the interval marked. The summary says where the difference is;
this says what the resampling actually produced -- whether it is symmetric,
skewed, or piled against a boundary, which the interval alone cannot show.
}
\examples{
d <- ilm_sim()
b <- ilm_boot_diff(d, "score", "grp", R = 300, seed = 1)
ilm_plot_boot_diff(b, row = 1)
}
\seealso{
\code{\link[=ilm_boot_diff]{ilm_boot_diff()}}.
}
