% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_boot.R, R/zzz-iml-aliases.R
\name{ilm_boot_ci}
\alias{ilm_boot_ci}
\alias{iml_boot_ci}
\title{Bootstrap confidence interval for a statistic}
\usage{
ilm_boot_ci(
  data,
  y = NULL,
  by = NULL,
  stat = "mean",
  R = 2000L,
  conf = 0.95,
  ci_type = "percentile",
  seed = NULL,
  progress = NULL
)
}
\arguments{
\item{data}{A data frame, or a numeric vector when \code{y} is \code{NULL}.}

\item{y}{Name of the numeric column to summarise.}

\item{by}{Optional grouping columns.}

\item{stat}{\code{"mean"}, \code{"median"}, \code{"sd"}, \code{"var"}, or a function taking a
numeric vector.}

\item{R}{Bootstrap replicates.}

\item{conf}{Confidence level.}

\item{ci_type}{\code{"percentile"}, \code{"bca"}, \code{"normal"} or \code{"basic"}. On skewed
data percentile and BCa hold their nominal coverage better than the other
two.}

\item{seed}{Random seed.}

\item{progress}{Show a progress bar. Defaults to \code{\link[=interactive]{interactive()}}, so a
bar appears when someone is watching and nothing is written in a
script or a knitted document. See \link{ilm_progress_arg}.}
}
\value{
A one-row data frame per group, with \code{observed}, \code{lower}, \code{upper}
and the settings used.
}
\description{
Bootstrap confidence interval for a statistic
}
\examples{
d <- ilm_sim()
ilm_boot_ci(d, "score", R = 200, seed = 1)
ilm_boot_ci(d, "score", by = "grp", R = 200, seed = 1)
}
\references{
Efron, B. and Tibshirani, R. J. (1993). An Introduction to the Bootstrap.
Chapman and Hall.
}
\seealso{
\code{\link[=ilm_boot_diff]{ilm_boot_diff()}} for a difference between two groups.
}
