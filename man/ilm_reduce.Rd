% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_reduce.R, R/zzz-iml-aliases.R
\name{ilm_reduce}
\alias{ilm_reduce}
\alias{iml_reduce}
\title{Reduce a data frame's variables to a few dimensions}
\usage{
ilm_reduce(data, cols = NULL, ndim = 5, method = c("pcamix", "glrm"), ...)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Columns to use. A character vector of names, a
regular expression, a predicate function such as \code{is.numeric}, or
\code{NULL} for all of them -- see \link{ilm_selection}.}

\item{ndim}{Number of dimensions to keep.}

\item{method}{\code{"pcamix"} (the default) for PCA, MCA or FAMD depending on
the column types, in closed form. \code{"glrm"} fits a generalized low rank
model instead, which uses a loss appropriate to each column's type rather
than squared error on one-hot indicators, and reconstructs a category as a
category. It costs an iterative fit, and on all-numeric data the two are
the same model -- see \code{\link[=ilm_glrm]{ilm_glrm()}} for when it is worth that.}

\item{...}{Passed to \code{\link[=ilm_glrm]{ilm_glrm()}} when \code{method = "glrm"}.}
}
\value{
An object of class \code{"ilm_reduce"}: \code{method} (\code{"pca"}, \code{"mca"} or
\code{"famd"}), \code{eig} (dimension, eigenvalue, percent of variance and its
cumulative total), \code{ind_coord} (\code{row_id} and one column per retained
dimension -- this is what \code{\link[=ilm_cluster]{ilm_cluster()}} takes), \code{var_contrib}
(\code{variable}, \code{dim}, \code{sqload}: how strongly each original variable relates
to each dimension, on a 0 to 1 scale, for numeric and categorical
variables alike), \code{n}, and \code{fit}, the underlying \code{PCAmixdata::PCAmix()}
object for anyone who wants to go past this wrapper.
}
\description{
Takes the columns of a data frame and summarises them as a small number of
dimensions. Which method that means is decided by the column types: PCA when
they are all numeric, multiple correspondence analysis when they are all
categorical, and a mixed method when both are present. A date or date-time
column cannot be used by any of the three and is dropped with a message;
convert it to something numeric first if it should count.
}
\details{
The mixed method is Chavent et al.'s, which belongs to the same
generalised-PCA family as the FAMD of Pages without being a
reimplementation of it. The two were checked against each other directly and
agree for this purpose, matching on eigenvalues and on individual
coordinates.
}
\examples{
r <- ilm_reduce(mtcars)
r
head(r$var_contrib[order(-r$var_contrib$sqload), ])
}
\references{
Chavent, M., Kuentz-Simonet, V., Labenne, A. and Saracco, J. (2014).
Multivariate analysis of mixed data: the PCAmixdata R package. arXiv
1411.4911.
}
\seealso{
\code{\link[=ilm_cluster]{ilm_cluster()}} to group the rows, \code{\link[=ilm_profile]{ilm_profile()}} for the whole
pipeline, \code{\link[=ilm_reduce_na]{ilm_reduce_na()}} for the same thing applied to missingness.
}
